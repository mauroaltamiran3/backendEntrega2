<nav class="navbar">
  <div class="nav-left">
    <a href="/" class="nav-link">🏠 Inicio</a>
    <a href="/realtimeproducts" class="nav-link">➕ Agregar Producto</a>
    <a href="/carts/{{carritoId}}" class="nav-link">🛒 Ir al Carrito</a>
    <a href="/info" class="nav-link">ℹ️ Info del Servidor</a>
  </div>

  <div class="nav-right">
  {{#if usuario}}
    <div class="nav-user">
      <span class="user">👤 {{usuario.first_name}} ({{usuario.role}})</span>
      {{#ifEquals usuario.role "user"}}
        <form action="/session/setadmin" method="POST" style="display:inline;">
          <button type="submit" class="nav-link" style="background:none; border:none; padding:0; cursor:pointer; color:white;">
            🔒 Convertirme en admin
          </button>
        </form>
      {{/ifEquals}}
            {{#ifEquals usuario.role "admin"}}
        <form action="/session/setuser" method="POST" style="display:inline;">
          <button type="submit" class="nav-link" style="background:none; border:none; padding:0; cursor:pointer; color:white;">
            🔒 Convertirme en user
          </button>
        </form>
      {{/ifEquals}}
      <a href="/session/logout" class="nav-link">🚪 Cerrar sesión</a>
    </div>
  {{else}}
    <a href="/login" class="nav-link" id="session-login-link">🔐 Iniciar sesión</a>
    <div id="jwt-navbar-placeholder" class="nav-user"></div>
  {{/if}}
</nav>